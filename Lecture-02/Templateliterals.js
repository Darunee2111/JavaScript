//Concatenation with template literals
console.log("In********concatenat ion with template literals********in");

let str1 = "JavaScript";

let str2 = "fun";

console, log(`I am writing code in sfstrly`);
console. log(`Formatting in $(str1) is $(str2)!`);

//Expressions in template literals
console.Log("In***k****Expressions in template literas*******in");

let booll = true;
console,log(`1 + 1 is$(1+1`);
console.Log(`The opposite of true is ${!booll} `);